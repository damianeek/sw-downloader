# ── Channel ────────────────────────────────────────────────────────────────────
# YouTube channel handle (without @)
CHANNEL_HANDLE=stan_wyjatkowy

# ── Output ─────────────────────────────────────────────────────────────────────
# Where downloaded videos are saved (mount this directory as a Docker volume)
OUTPUT_DIR=/downloads

# Path of the state file (tracks URL found + download status across cron runs)
# Default: <OUTPUT_DIR>/state.json
STATE_FILE=/downloads/state.json

# ── Binaries ───────────────────────────────────────────────────────────────────
# Full path to yt-dlp binary (leave empty to use PATH)
YTDLP_BIN=yt-dlp

# Full path to ffmpeg binary — passed to yt-dlp via --ffmpeg-location
# Required if ffmpeg is not in PATH
FFMPEG_LOCATION=/usr/bin/ffmpeg

# ── Schedule ───────────────────────────────────────────────────────────────────
# Year the show premiered — used in filenames:
#   "Stan Wyjątkowy (2022) - 2026-03-14 - Title [id].mp4"
# Set to empty to use yt-dlp default: "Uploader - Title [id].mp4"
SHOW_YEAR=2022

# Max age of a video (in hours) to be considered the unedited stream.
# The channel edits the VOD after 12-24h, so keep this below that window.
MAX_AGE_HOURS=12

# Minimum video duration in minutes to be considered the unedited stream.
# After editing the VOD is cut short, so anything under this is skipped.
MIN_DURATION_MINUTES=60

# Cron expression: when to check if the stream is live and grab the URL
# Default: Saturday 20:05
FIND_CRON=5 20 * * 6

# Cron expression: how often to attempt the download after stream is found
# Default: every 30 minutes
DOWNLOAD_CRON=*/30 * * * *

# Timezone for all cron jobs
TIMEZONE=Europe/Warsaw

# ── Debug ──────────────────────────────────────────────────────────────────────
# Set to true to run both jobs immediately instead of waiting for cron
RUN_NOW=false

# Set to false to show the Playwright browser window (local debugging only)
HEADLESS=true
